import React, { useState } from "react";

export const TopBar: React.FC = () => {
  return (
    <div className="sticky top-0 left-0 w-full h-12 z-30 py-2 px-3 flex bg-gray-100 border-b border-gray-400"></div>
  );
};
